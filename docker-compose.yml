services:
  cloudflare-solver:
    build:
      context: .
      platform: linux/amd64
    ports:
      - "3000:3000"
      - "8080:8080"
    environment:
      PORT: 3000
      NODE_ENV: production
    shm_size: "1gb"
    restart: unless-stopped
